language: python
python:
  - "2.7"



install: true

# Should be set to PyPI package on next release
before_install:
   # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq


install:
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

  - pip install git+https://github.com/ioam/param.git

script:
  - nosetests --with-doctest --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_lancet_713ce8faee_2
  - python -m properform Push 47.88.50.110:15554 asdf_lancet_713ce8faee_2 ./profile_lancet_713ce8faee_2
 #2017-09-16 13:42:59 modified at 0 #2017-09-16 13:43:07 modified at 1 #2017-09-16 13:43:13 modified at 2