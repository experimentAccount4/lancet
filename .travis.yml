language: python
python:
  - "2.7"



install: true

# Should be set to PyPI package on next release
before_install:
   # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq


install:
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

  - pip install git+https://github.com/ioam/param.git

script:
  - nosetests --with-doctest --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_lancet_9b85cf749b_1
  - python -m properform Push 47.88.50.110:15554 asdf_lancet_9b85cf749b_1 ./profile_lancet_9b85cf749b_1
 #2017-09-16 13:38:44 modified at 0 #2017-09-16 13:38:50 modified at 1